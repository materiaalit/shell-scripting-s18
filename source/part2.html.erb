---
  title: Osa 2
  exercise_page: true
  quiz_page: true
  published: true
---

Tuntee loogiset operaatiot <code>ja</code> ja <code>tai</code> ja osaa käyttää niitä ehtolauseissa. 

<% partial 'partials/material_heading' do %>
  Loogiset operaatiot
<% end %>


<% partial 'partials/material_heading' do %>
  Ohjelman pilkkominen osiin: metodit
<% end %>



- crowdsorcerer, metodien testaus, yhä koodi muuttumatta?


hint: ohjelman suorituksen lopettaminen.

Googlella saattaa löytyä...

One should NEVER call System.exit(0).

It is a hidden "goto", and "gotos" breaks the control flow. Relying on hooks in this context is a mental mapping every developer in the team has to be aware of.
Quitting the program "normally" will provide the same exit code to the operating system as System.exit(0). So it is redundant. If your program cannot quit "normally", your development got out of control. You should have always full control of the system state.
You hide programming problems such as running threads that are not stopped normally.
This refers to 3.: You may encounter an inconstistent application state interrupting threads abnormally.